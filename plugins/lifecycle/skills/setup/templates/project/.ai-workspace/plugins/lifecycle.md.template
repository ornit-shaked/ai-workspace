# lifecycle

Installed [install-date]

## Files
- `work-state.md` — Features table with lifecycle gates
- `.features/<id>/` — feature.md, spec.md, plan.md, tasks.md

## Lifecycle
idea → feature (WHAT+WHY) → spec (HOW) → plan (order) → tasks (executable) → done

[Plugin Documentation](https://github.com/ornit-shaked/ai-workspace/tree/main/plugins/lifecycle)
