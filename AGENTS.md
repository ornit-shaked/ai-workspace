# Agent Instructions — ai-workspace

**See `CLAUDE.md` for full project context and how to work on this project.**

## Quick Reference
- Test changes: `node index.js install project-brain ~/code/test-project`
- Plugin templates are in `plugins/*/global/` and `plugins/*/project/` (use `.template.md` extension)
- Template files are NOT active config for this project - they're copied during installation
